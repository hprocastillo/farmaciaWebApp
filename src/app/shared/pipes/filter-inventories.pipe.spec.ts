import { FilterInventoriesPipe } from './filter-inventories.pipe';

describe('FilterInventoriesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterInventoriesPipe();
    expect(pipe).toBeTruthy();
  });
});
