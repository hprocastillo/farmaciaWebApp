import { FilterStoresPipe } from './filter-stores.pipe';

describe('FilterStoresPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterStoresPipe();
    expect(pipe).toBeTruthy();
  });
});
